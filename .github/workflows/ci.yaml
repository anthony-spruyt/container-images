---
# yaml-language-server: $schema=https://raw.githubusercontent.com/SchemaStore/schemastore/master/src/schemas/json/github-workflow.json
name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    paths:
      # Image files
      - "**/Dockerfile"
      - "**/metadata.yaml"
      - "**/flavor.yaml"
      - "**/assets/**"
      # MegaLinter factory (triggers all flavor rebuilds)
      - "megalinter-factory/**"
      # CI scripts (trigger script tests)
      - ".github/scripts/**"
      - ".github/workflows/_image-pipeline.yaml"
      - "test-images/**"
  workflow_dispatch:
    inputs:
      image:
        description: "Image to build (directory name, e.g., 'chrony')"
        required: true
        type: string
      version:
        description: "Version tag (optional, uses metadata.yaml version if not specified)"
        required: false
        type: string
      dry_run:
        description: "Dry run mode - build and scan but skip push and release"
        required: false
        type: boolean
        default: true

permissions:
  attestations: write
  contents: write
  id-token: write
  packages: write
  pull-requests: write
  security-events: "write"
  statuses: write

env:
  REGISTRY: ghcr.io
  OWNER: ${{ github.repository_owner }}

jobs:
  lint:
    uses: "anthony-spruyt/repo-operator/.github/workflows/_lint.yaml@main"
    secrets: "inherit"

  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      images: ${{ steps.detect.outputs.images }}
      scripts: ${{ steps.detect.outputs.scripts }}
    steps:
      # SHA pin: actions/checkout@v4.3.1
      - name: Checkout
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          fetch-depth: 0

      - name: Detect changed images and scripts
        id: detect
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_IMAGE: ${{ inputs.image }}
        run: |
          # Detect CI script changes (for test-ci-scripts job)
          detect_scripts() {
            local diff_cmd="$1"
            if $diff_cmd | grep -qE '^\.github/scripts/|^\.github/workflows/_image-pipeline\.yaml|^test-images/'; then
              echo "true"
            else
              echo "false"
            fi
          }

          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            # Manual dispatch: use specified image, no script tests
            if ! echo "$INPUT_IMAGE" | grep -qE '^[a-zA-Z0-9_-]+$'; then
              echo "::error::Invalid image name format"
              exit 1
            fi
            if [ ! -d "$INPUT_IMAGE" ]; then
              echo "::error::Image directory does not exist: $INPUT_IMAGE"
              exit 1
            fi
            echo "images=[\"$INPUT_IMAGE\"]" >> "$GITHUB_OUTPUT"
            echo "scripts=false" >> "$GITHUB_OUTPUT"
            echo "Manual dispatch for image: $INPUT_IMAGE"

          elif [ "$EVENT_NAME" = "pull_request" ]; then
            # PR: compare against base branch
            DIFF_FILES=$(git diff --name-only origin/main...HEAD)

            # Collect changed images from direct file changes
            DIRECT_CHANGES=$(echo "$DIFF_FILES" | \
              grep -E '^[a-zA-Z0-9_-]+/(Dockerfile|test\.sh|assets/|metadata\.yaml|flavor\.yaml)' | \
              cut -d'/' -f1 | sort -u | \
              while read -r img; do [ -d "$img" ] && echo "$img"; done)

            # If megalinter-factory changed, add all flavor directories
            if echo "$DIFF_FILES" | grep -qE '^megalinter-factory/'; then
              echo "MegaLinter factory changed - adding all flavors to build"
              FLAVOR_DIRS=$(find . -maxdepth 2 -name 'flavor.yaml' -printf '%h\n' | sed 's|^\./||')
              ALL_IMAGES=$(printf '%s\n%s' "$DIRECT_CHANGES" "$FLAVOR_DIRS" | sort -u | grep -v '^$')
            else
              ALL_IMAGES="$DIRECT_CHANGES"
            fi

            CHANGED=$(echo "$ALL_IMAGES" | jq -R -s -c 'split("\n") | map(select(length > 0))')
            SCRIPTS=$(detect_scripts "echo \"$DIFF_FILES\"")

            echo "images=${CHANGED:-[]}" >> "$GITHUB_OUTPUT"
            echo "scripts=$SCRIPTS" >> "$GITHUB_OUTPUT"
            echo "Changed images: ${CHANGED:-[]}, Scripts changed: $SCRIPTS"

          else
            # Push to main: compare against previous commit
            DIFF_FILES=$(git diff --name-only HEAD~1 HEAD)

            # Collect changed images from direct file changes
            DIRECT_CHANGES=$(echo "$DIFF_FILES" | \
              grep -E '^[a-zA-Z0-9_-]+/(Dockerfile|metadata\.yaml|flavor\.yaml|assets/)' | \
              cut -d'/' -f1 | sort -u | \
              while read -r img; do [ -d "$img" ] && echo "$img"; done)

            # If megalinter-factory changed, add all flavor directories
            if echo "$DIFF_FILES" | grep -qE '^megalinter-factory/'; then
              echo "MegaLinter factory changed - adding all flavors to build"
              FLAVOR_DIRS=$(find . -maxdepth 2 -name 'flavor.yaml' -printf '%h\n' | sed 's|^\./||')
              ALL_IMAGES=$(printf '%s\n%s' "$DIRECT_CHANGES" "$FLAVOR_DIRS" | sort -u | grep -v '^$')
            else
              ALL_IMAGES="$DIRECT_CHANGES"
            fi

            CHANGED=$(echo "$ALL_IMAGES" | jq -R -s -c 'split("\n") | map(select(length > 0))')
            SCRIPTS=$(detect_scripts "echo \"$DIFF_FILES\"")

            echo "images=${CHANGED:-[]}" >> "$GITHUB_OUTPUT"
            echo "scripts=$SCRIPTS" >> "$GITHUB_OUTPUT"
            echo "Changed images: ${CHANGED:-[]}, Scripts changed: $SCRIPTS"
          fi

  test-ci-scripts:
    name: Test CI Scripts
    needs: [detect-changes, lint]
    runs-on: ubuntu-latest
    # Run if CI scripts changed AND (lint passed OR lint was skipped)
    if: |
      always() &&
      needs.detect-changes.outputs.scripts == 'true' &&
      (needs.lint.result == 'success' || needs.lint.result == 'skipped')
    steps:
      # SHA pin: actions/checkout@v4.3.1
      - name: Checkout
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98

      - name: Test version fixtures
        run: .github/scripts/test-version-fixtures.sh

      - name: Test changelog generation
        run: .github/scripts/test-changelog.sh

  build:
    name: Build ${{ matrix.image }}
    needs: [detect-changes, lint]
    # Run if we have images AND (lint passed OR lint was skipped for renovate)
    if: |
      always() &&
      needs.detect-changes.outputs.images != '[]' &&
      (needs.lint.result == 'success' || needs.lint.result == 'skipped')
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.detect-changes.outputs.images) }}
    uses: ./.github/workflows/_image-pipeline.yaml
    secrets: inherit
    with:
      image: ${{ matrix.image }}
      # Only push/release on main push or manual dispatch with dry_run=false
      push: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run != 'true') }}
      create-release: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run != 'true') }}
      version-override: ${{ inputs.version || '' }}

  # Summary job for branch protection - add jobs to 'needs' array
  summary:
    needs:
      - detect-changes
      - lint
      - test-ci-scripts
      - build
    if: "always()"
    uses: "anthony-spruyt/repo-operator/.github/workflows/_summary.yaml@main"
